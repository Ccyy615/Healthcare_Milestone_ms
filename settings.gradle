rootProject.name = 'milestone-healthcare-ms'

[
        'appointment-service',
        'clinic-room-service',
        'doctor-service',
        'patient-service',
        'apigateway-service'
].each { serviceName ->
    if (file(serviceName).isDirectory()) {
        include serviceName
    }
}
